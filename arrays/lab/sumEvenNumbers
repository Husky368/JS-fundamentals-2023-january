function sumEvenNumbers(input){
    let sum = 0
    for (let index = 0; index <= input.length-1; index++) {
        if(input[index]%2==0){
            sum+=Number(input[index])
        }
    }
    console.log(sum);
}
